{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "smoke:operational": "node scripts/smoke-operational.mjs",
    "sync:skills": "node scripts/sync-external-skills.mjs",
    "test:skills": "pnpm --filter @workspace/api-server run build && node tests/skills-service.test.mjs",
    "db:ensure-operational-schema": "pnpm --filter @workspace/db run ensure-operational-schema",
    "test:operational-schema": "node tests/operational-schema.test.mjs",
    "test:execution": "pnpm --filter ./scripts exec tsx --test ../tests/execution-policy.test.ts",
    "test:workflow": "node --test tests/inbox-review-workflow.test.mjs tests/inbox-review-timer.test.mjs tests/mission-control-surface.test.mjs tests/dashboard-calendar-projects.test.mjs && pnpm --filter ./scripts exec tsx --test ../tests/task-completion-policy.test.ts"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}